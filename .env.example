# Variables docker compose substitutes into docker-compose.yml. Copy to .env.
# The admin account does NOT belong here - see secrets.env.example (F-58).

# The uid/gid the containers run as; must own ./data (F-45).
UID=1000
GID=1000

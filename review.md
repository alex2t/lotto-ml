# Comprehensive Codebase Review, Logic Audit & System Automation Plan

**Date:** September 2026  
**System:** Irish Lotto ML Analysis & Prediction System (v3.16b)  
**Mode:** Audit, Verification & Implementation (All Issues 1 through 7 Resolved and Verified; Dataset Fully Updated Through September 2026).

---

## 1. System Architecture & Codebase Overview

The repository implements an end-to-end lottery analysis, machine learning prediction, and visual inspection suite tailored for the Irish National Lottery (6/47 format + 1 bonus ball).

```
┌─────────────────┐      ┌─────────────────────────┐      ┌─────────────────────────┐
│ irish500.csv    │ ───► │ drawpick.py             │ ───► │ 17 Validated JSON Files │
│ (Historical)    │      │ (16 Analysis Phases)    │      │ (data/ directory)       │
└─────────────────┘      └─────────────────────────┘      └────────────┬────────────┘
                                                                       │
                         ┌─────────────────────────┐                   │
                         │ lottery_picks.txt       │ ◄─────────────────┤
                         │ (Generated Predictions) │                   │
                         └─────────────────────────┘                   ▼
                                                          ┌─────────────────────────┐
                         ┌─────────────────────────┐      │ quickpick.py            │
                         │ app.py                  │ ◄─── │ (Feature extraction,    │
                         │ (8-Page Streamlit UI)   │      │  training & selection)  │
                         └─────────────────────────┘      └─────────────────────────┘
```

### Core Subsystems
1. **Raw Ingestion (`data/irish500.csv`)**: Historical draw archive stored in reverse chronological order (`Date,Num1,Num2,Num3,Num4,Num5,Num6,Bonus`).
2. **Statistical Engine (`drawpick.py` & `lotto_analysis/`)**:
   - Executes 16 sequential analytical phases (HMC classification, trigger series, freshness bins, odd/even splits, sum distributions, bonus-to-main transitions, scipy validation tests).
   - Generates 17 JSON artifact files (~4.3 MB) into `data/`.
3. **Machine Learning Pipeline (`quickpick.py` & `ml_lotto/`)**:
   - **Model 1 (`Momentum Specialist`)**: Logistic Regression focusing on short-term recency, momentum, rolling rates, and pairwise/triple interactions.
   - **Model 2 (`Jackpot Optimizer`)**: Random Forest trained strictly on the 6 main balls (excluding bonus) focusing on long-term stability and distribution balance.
   - **Model 3 (`Complexity Explorer`)**: XGBoost trained on all features across all 7 drawn balls to capture non-linear feature interactions.
   - **Model 4 (`Pool Generator`)**: CatBoost model trained on high-importance features to output a 20-number candidate pool.
   - **Auxiliary Models**: Dedicated Bonus Ball Logistic Regressor and Bonus-to-Main Transition Predictor (capturing the empirical 74% 10-draw transition rate).
   - **Selection Layer (`ilp_selection.py`, `filters.py`, `constraints.py`)**: ILP selection balancing HMC ratios, freshness constraints, and mathematical bounds (sum range 84–206, odd/even balance 2:4 to 4:2, range dispersion).
4. **Visual Dashboard (`app.py` & `view/pages/`)**:
   - 8-page Streamlit application providing trigger analysis, draw history, statistical distributions, freshness matrices, prediction validation, deep number insights, pattern similarity, and post-draw model evaluation.

---

## 3. Actionable Improvements for Prediction Quality

Lottery numbers are generated by a uniformly distributed physical random process ($p = 6/47 pprox 12.77\%$ for main, $7/47 pprox 14.89\%$ including bonus). True entropy cannot be beaten. **No change to the models will improve prediction**, and all six currently sit at chance (validation AUC 0.498-0.545), which is the correct result rather than a shortfall.

That rules out most of what once stood here. Three proposals were removed on 2026-09-18 because each
depended on signal that does not exist; see the reasoning below and in
[`docs/feature_review.md`](docs/feature_review.md). The two that remain are kept because **neither
requires the models to predict anything.**

| Removed | Why |
|:--|:--|
| Dynamic Walk-Forward Feature Generation | Its leakage case reduced to one feature (`window_saturation_penalty`) in one auxiliary model scoring below chance. The other five it named are computed and discarded, unused by any model. Its two headline gains were already banked (C-15b, and Model 3's top feature is no longer `series_recent`). Full assessment in `docs/feature_review.md`; the plan document is deleted and recoverable at commit `28ac724`. |
| Soft-Voting Ensemble & Isotonic Calibration | Averaging four models that are all at chance yields a model at chance. The calibration half solves a problem the system does not have - errors are 0.0105-0.0231, well inside the 0.05 "good" band - and isotonic overfits more readily than the sigmoid already in use on a 60-draw window. See F-6. |
| Learning-to-Rank (LambdaMART / YetiRank) | Changing the loss function does not create signal. NDCG@7 over a uniform draw has the same ceiling as AUC 0.5; optimising a ranking objective on noise returns a well-ranked list of noise. The genuine benefit is metric interpretability, which does not justify a seventh model. |

---

### 1. Combinatorial Line Generation from the Model 4 Pool (Covering Designs / Wheeling)

> **Implemented 2026-09-18.** `ml_lotto/prediction/wheel.py` wheels the top 8 of the pool into 4
> lines covering every 3-subset, guarded by `tests/test_wheel.py`. Two claims below turned out wrong
> and are corrected in place: a guarantee over the full 20 does not fit 4-8 lines, and a wheel does
> not mean fewer blanks.

* **Previous state:** Model 4 emitted a ranked 20-number pool and **no lines at all**. Converting it into tickets was left to the user, by hand.
* **Improvement:** Apply covering designs to turn the pool into a stated budget of lines (e.g. 4-8) carrying a provable guarantee - for instance, that if enough winners fall inside the 20, at least one line catches 3 of them.

**Why this is worth doing.** It is the only item here whose benefit is **proved rather than hoped
for**. A covering design is a conditional guarantee that holds for every possible draw: it makes no
claim about which numbers appear, only that *whatever* appears, a sufficient overlap with the pool
converts into a prize. Luck decides whether the condition fires; arithmetic decides the payout once
it does.

It closes a real failure mode. Five winners can sit inside the pool and still return nothing if the
lines are chosen casually, because no single line holds three of them. Wheeling removes that
possibility.

**Be honest about what it is not.** It does not increase expected value, which stays negative.
Expected winners in any 20 of 47 is 2.553 - identical for a random 20, since Model 4's AUC of 0.5118
is inside the +/-0.031 noise band. Wheeling reshapes the outcome distribution, but toward **fewer,
clustered** wins, not fewer blanks: every line sits inside the same few numbers, so 4 wheel lines
catch a match-3 in ~5.3% of draws where 4 unrelated lines do in ~7.2%, at identical expected value.
It is variance management, matching the "portfolio-allocation" role stated above, and should never
be presented as an edge.

**Design note.** Pick the line budget first, then the covering design that fits it. Covering every
3-subset of the full 20 needs 60+ lines, and even the weakest useful guarantee - all 6 winners in
the pool - needs more than 8 lines and fires in 0.36% of draws. What fits the budget is a full
3-cover of the pool's top *k*:

| Top-k | Lines | Guarantee fires (3+ winners in top-k) |
|--:|--:|--:|
| 8 | 4 | 5.3% |
| 9 | 7 | 7.5% |
| 10 | 10 | 10.1% |
| 12 | 15 | 16.4% |

Top-8 / 4 lines was chosen. The unit test asserts every 3-subset of the top 8 appears in some line.

---

### 2. Multi-Objective Constraint Optimization (Integer Linear Programming)

> **Implemented 2026-09-18.** `ml_lotto/prediction/ilp_selection.py` replaces `selection.py` and the
> repair pass, using `scipy.optimize.milp`. HMC quotas stay exact per model (the dashboard in `view/`
> keeps showing HMC categories unchanged). On sequencing, the freshness target is encoded **as it
> is** - the reachable pattern `constraints.reachable_pattern()` already computed - so the frozen
> mechanism is enforced, not changed. Deleting it later means deleting one constraint block. See F-14
> in `issue.md` for the before/after.

* **Improvement:** Replace greedy HMC picking in `selection.py` with an ILP solver (`scipy.optimize.milp` or `pulp`):
  $$\max \sum_{i=1}^{47} P_i \cdot x_i$$
  subject to
  $$\sum x_i = 6, \quad 84 \le \sum (	ext{val}_i \cdot x_i) \le 206, \quad 2 \le \sum (	ext{odd}_i \cdot x_i) \le 4$$
  $$x_i \in \{0, 1\}, \quad 	ext{HMC bounds, freshness bounds}$$

**Why this is worth doing.** Not for prediction - it will not move a single metric. It is a
**defect-surface** improvement, and the selection layer is by some distance the buggiest part of this
system: F-1, F-8, F-13, C-2, C-11, C-12 and N-4 all originated there.

The reason is structural. Greedy picking plus a repair pass encodes constraints *procedurally*, so a
constraint can appear to be satisfied while never actually binding - which is precisely how the
freshness target was consulted for ordering and then discarded for as long as it was (F-8). An ILP
states the constraints *declaratively*. The solver either returns a line meeting all of them or
reports infeasibility.

That would have made two of this system's defects impossible rather than merely fixed: an unreachable
freshness target becomes an infeasibility reported at once, instead of a silent miss (F-13). It also
removes the repair path, which was observed scrambling the freshness distribution while it patched an
odd/even failure.

**Sequencing.** Worth doing only once it is settled which constraints should exist. Encoding the
freshness target into a solver while that mechanism is under change freeze would harden a rule that
may be deleted - see improvement 4 in `issue.md`. Resolve that first, then encode what survives.

---

## 4. Web Scraping Planning: Irish National Lottery

### Target Source Analysis

#### Option A: Official Source (`https://www.lottery.ie/results/lotto/history`) — **Recommended Primary**
* **Verification:** Confirmed via live fetch.
* **Page Type:** Server-Side Rendered (SSR) HTML via Next.js.
* **Draw Data Structure:**
  - Header: `<h2 aria-label="Draw, Saturday, September 12th, 2026" class="font-black text-2xl">Sat 12/09/26</h2>`
  - Jackpot: `<p class="font-black text-xl">€2,776,174</p>`
  - 6 Main Balls: `<div class="flex gap-1.5 md:gap-2 flex-wrap">` containing 6 ball elements with numbers `1, 3, 9, 14, 16, 19`.
  - Bonus Ball: `<div class="flex-col"><div class="leading-5 font-bold py-1.5">Bonus</div>` containing ball with number `27`.
* **Reliability:** Direct official source, includes last 90 days of history.

#### Option B: Clean Secondary Source (`https://www.irishlottery.com/results`) — **Automatic Fallback**
* **Verification:** Confirmed accessible via live fetch.
* **Page Type:** Standard HTML.
* **Role:** Secondary fallback if `lottery.ie` implements anti-bot challenges (Cloudflare / Akamai).

---

### Draw Timing & Scheduling
* **Draw Days:** Every **Wednesday** and **Saturday** evening.
* **Draw Execution:** 20:00 Irish Time (GMT in Winter, IST/UTC+1 in Summer).
* **Results Publication Window:** Typically published between **20:45 and 21:15**.
* **Recommended Polling Strategy:**
  - Cron trigger at **21:05**.
  - If results for today's date are not yet published, retry with exponential backoff (21:15, 21:30, 22:00) up to a maximum timeout.

---

### Scraping Engine Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                 Scraper Orchestrator                        │
│                 (scripts/scrape_lotto.py)                   │
└──────────────────────────────┬──────────────────────────────┘
                               │
               ┌───────────────┴───────────────┐
               ▼                               ▼
       [Primary Scraper]              [Fallback Scraper]
       lottery.ie/results/history     irishlottery.com/results
               │                               │
               └───────────────┬───────────────┘
                               ▼
               ┌───────────────────────────────┐
               │ Data Validation & Formatting  │
               │ - Exactly 6 main (1-47)       │
               │ - Exactly 1 bonus (1-47)      │
               │ - Unique balls (len == 7)     │
               │ - Date parsing -> DD Mon YYYY │
               └───────────────┬───────────────┘
                               ▼
               ┌───────────────────────────────┐
               │ Duplicate Detection           │
               │ (Check latest date in CSV)    │
               └───────────────┬───────────────┘
                               ▼
               ┌───────────────────────────────┐
               │ Atomic Update to              │
               │ data/irish500.csv             │
               └───────────────────────────────┘
```

#### Scraping Robustness Protocol:
1. **HTTP Headers:** Modern realistic browser headers (`User-Agent: Mozilla/5.0...`, `Accept-Language: en-IE,en-US;q=0.9`).
2. **Strict Schema Validation:**
   - Numbers must be integers $\in [1, 47]$.
   - Main balls list length $\equiv 6$, Bonus $\equiv 1$, Total set length $\equiv 7$.
3. **Date Harmonization:**
   - Convert parsed date (e.g. `Sat 12/09/26`) to canonical CSV format: `12 Sep 2026`.
4. **Duplicate Prevention:**
   - Read top row of `data/irish500.csv`. If scraped date matches top row, terminate cleanly with log: `"Latest draw already ingested. No update required."`
5. **Atomic Prepending:**
   - Prepend new record directly after header row to maintain strict descending order required by `data_loader.py`.

---

## 5. VPS Hosting & Full Automation Architecture

To achieve full end-to-end automation on your VPS, the system should operate without manual intervention: scraping new draws, updating analysis, retraining models, generating picks, logging metrics, and hosting the dashboard continuously.

### Infrastructure Blueprint

```
                     ┌──────────────────────────────────────────────┐
                     │                  Your VPS                    │
                     │          (Ubuntu 22.04/24.04 LTS)            │
                     └──────────────────────┬───────────────────────┘
                                            │
               ┌────────────────────────────┼────────────────────────────┐
               ▼                            ▼                            ▼
   ┌───────────────────────┐    ┌───────────────────────┐    ┌───────────────────────┐
   │ Nginx Reverse Proxy   │    │ Systemd Timer / Cron  │    │ Streamlit Service     │
   │ (Ports 80/443 + SSL)  │    │ (Wed & Sat @ 21:05)   │    │ (systemd: port 8501)  │
   └───────────┬───────────┘    └───────────┬───────────┘    └───────────▲───────────┘
               │                            │                            │
               │                            ▼                            │
               │                ┌───────────────────────┐                │
               │                │ scripts/auto_pipeline │                │
               │                │ .py                   │                │
               │                └───────────┬───────────┘                │
               │                            │                            │
               │                1. Scrape & Update CSV                   │
               │                2. Execute drawpick.py                   │
               │                3. Evaluate Post-Draw                    │
               │                4. Execute quickpick.py                  │
               │                5. Send Alerts/Telegram                  │
               │                            │                            │
               │                            └────────────────────────────┘
               ▼                                       Reloads data
       Internet Users
```

---

### Key Automation Components

#### 1. Automated Execution Pipeline (`scripts/auto_pipeline.py`)
A single master command triggered automatically post-draw:
```bash
python scripts/auto_pipeline.py
```
**Execution Sequence:**
1. **Scrape**: Fetches latest winning numbers from `lottery.ie`.
2. **Check**: If new draw exists $\rightarrow$ prepends to `data/irish500.csv`. If no new draw $\rightarrow$ exits gracefully.
3. **Analyze**: Runs `drawpick.py` to regenerate all 17 statistical JSON files.
4. **Evaluate (Post-Draw)**: Compares newly drawn numbers against previous `lottery_picks.txt` and logs performance metrics to `data/model_performance_history.json`.
5. **Predict**: Runs `quickpick.py` to train models on the updated dataset and write new predictions to `lottery_picks.txt`.
6. **Notify**: Sends Telegram/Discord/Email notification with:
   - Match results for the draw just completed.
   - Validation score and generated picks for the upcoming draw.

#### 2. Systemd Timer (or Linux Cron)
Configured for Wednesday and Saturday nights:
```ini
# /etc/systemd/system/lotto-pipeline.timer
[Unit]
Description=Run Irish Lotto Ingestion and Prediction Pipeline

[Timer]
OnCalendar=Wed,Sat 21:05:00 Europe/Dublin
Persistent=true

[Install]
WantedBy=timers.target
```

#### 3. Streamlit Dashboard as a Background Service
Managed cleanly by `systemd`:
```ini
# /etc/systemd/system/lotto-dashboard.service
[Unit]
Description=Irish Lotto Streamlit Dashboard
After=network.target

[Service]
User=www-data
WorkingDirectory=/var/www/lotto-ml-system
ExecStart=/var/www/lotto-ml-system/venv/bin/streamlit run app.py --server.port 8501 --server.address 127.0.0.1 --server.headless true
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

#### 4. Nginx Reverse Proxy with HTTPS
- Reverse proxies incoming public traffic on port `80`/`443` to `http://127.0.0.1:8501`.
- Enables WebSocket streaming (`proxy_set_header Upgrade $http_upgrade;`).
- Automated free SSL certificates via Let's Encrypt (`certbot --nginx`).
- Optional HTTP Basic Authentication (`htpasswd`) to password-protect the dashboard on the public web.

#### 5. Automated Data Backups & Git Versioning
- Post-pipeline git auto-commit to a private GitHub/GitLab repository or local backup directory:
  ```bash
  git add data/irish500.csv lottery_picks.txt
  git commit -m "Auto-update: Draw results for $(date +%Y-%m-%d)"
  ```

---

## 6. Phased Implementation Roadmap

When you are ready to proceed with implementation, the work should be structured in the following sequential phases:

```
Phase 1: Logic & Selection Bug Fixes
  ├── Fix selection.py fallback and eliminate hard exclusion deadlocks
  ├── Harmonize config.py Model 1 parameters with documentation
  └── Resolve double diversity penalty conflict in predictor.py

Phase 2: Point-in-Time Training Matrix & ML Enhancement
  ├── Refactor trainer.py to build true walk-forward temporal feature dataset
  ├── Implement soft-voting probability ensemble
  └── Connect Model 4 pool to constrained combinatorial line generator

Phase 3: Web Scraper & Auto-Ingestion Module
  ├── Develop scripts/scraper.py with lottery.ie parser & irishlottery.com fallback
  ├── Implement data sanitizer, duplicate check, and atomic CSV update
  └── Unit test scraper with network mocks and live fetch verification

Phase 4: Automated Master Pipeline & Notifications
  ├── Build scripts/auto_pipeline.py chaining scrape -> drawpick -> quickpick
  ├── Integrate automated post-draw evaluation logging
  └── Add Telegram / Discord webhook notification alerts

Phase 5: VPS Deployment & Production Hardening
  ├── Setup Python venv and system dependencies on VPS
  ├── Configure systemd service for Streamlit app
  ├── Configure systemd timer / cron for Wed/Sat 21:05 automated execution
  └── Configure Nginx reverse proxy with SSL (Certbot) and security headers
```

### Where Phases 1 and 2 stand (verified 2026-09-18)

Checked against the code, `issue.md` and a full `quickpick.py` run. The original write-ups behind
these steps were never committed, so each is judged by what the code does today.

| Phase | Step | Status | Go ahead? |
|:--|:--|:--|:--|
| 1 | Fix `selection.py` fallback, eliminate hard exclusion deadlocks | **Done, then superseded** | - |
| 1 | Harmonise Model 1 parameters with documentation | **Not started** - logged as F-15 | Yes, small |
| 1 | Resolve double diversity penalty in `predictor.py` | **Not started** - logged as F-16 | Yes, small |
| 2 | True walk-forward training dataset | **Done** | - |
| 2 | Soft-voting probability ensemble | **Rejected** | No |
| 2 | Model 4 pool -> combinatorial line generator | **Done** | - |

**Phase 1 - selection fallback and deadlocks: done.** The hard lockout was removed (C-9), the
fallback that overfilled a line (C-11) and the top-up that duplicated a pre-assigned number (C-12)
were fixed. `selection.py` itself is now gone: the ILP (§3.2) replaced it, and it cannot deadlock -
a penalised number is avoided but never banned, and an impossible set of constraints raises instead
of returning a short line.

**Phase 1 - harmonise Model 1's config: not started (F-15).** Models 1, 2 and 3 list `recent_14`,
which the serving path never produces, so `expand_feature_selection` drops it without a word (Model 1
trains on 25 features, none of them `recent_14`). Model 1's comments also misdescribe it: "~17
features", `recent_4` as "last 4 draws" (it is 5), and an empty "CONSTRAINTS" block.
*How:* delete `recent_14` from the three configs, correct the comments, and make
`expand_feature_selection` raise on a name that is neither a keyword nor a produced feature, so the
next typo fails loudly. *Worth it:* yes - under an hour, no model changes (the feature was never
used), and it closes the silent-default class this repo keeps hitting.

**Phase 1 - double diversity penalty: not started (F-16).** `predictor.py` lowers each penalised
number's probability by the configured `diversity_penalty` (25-40%, rank-aware), then `solve_line`
also subtracts 1 per penalised number. The flat 1 dominates, so the configured percentages barely
matter: they only order penalised numbers among themselves when one is unavoidable.
*How:* keep one mechanism. Recommended: keep the flat cost (lines as disjoint as the constraints
allow), delete `apply_rank_aware_penalty` and the `diversity_penalty` keys, and pass raw probabilities
to the solver. With every model at chance, rank carries no information; disjoint lines at least
cover more numbers for the same stake. *Worth it:* yes, but for honesty rather than results - the
config should not advertise a 30% soft penalty that is effectively a near-hard rule. Picks may change
for Models 2 and 3; metrics will not.

**Phase 2 - walk-forward training dataset: done.** `ml_lotto/features/walk_forward.py` builds every
training row from draws before it (C-3, C-4, C-5, N-1, N-1b), `tests/test_walk_forward_parity.py`
pins train/serve agreement, and since C-15a one engine serves the whole run. The follow-on proposal
(Dynamic Walk-Forward for the remaining static features) was assessed and dropped - see §3.

**Phase 2 - soft-voting ensemble: rejected.** Averaging four models that are all at chance gives a
model at chance (§3). Do not implement. The dead hard-voting code it would build on is F-6 - decide
there whether to delete it.

**Phase 2 - Model 4 pool to line generator: done.** `ml_lotto/prediction/wheel.py` turns the pool's
top 8 into 4 covering lines (§3.1). They are deliberately *not* passed through the ticket filters:
repairing one line would break the guarantee.

**Next:** F-15 and F-16 finish Phase 1 and are both cheap. Phase 3 (scraper) is already largely
built - `scripts/scrape_lotto.py` has two sources and tests (C-14) - so Phase 4 automation is the
next substantive work.

---

*Sections 2 and 7 (the logic-error audit and the code-review response) have been removed.
Outstanding defects are tracked in [`issue.md`](issue.md). Section numbering is left unchanged
so existing cross-references still resolve.*
